{
  "name": "npm-monorepo-fossa-test",
  "version": "1.0.0",
  "private": true,
  "description": "Test JavaScript monorepo with multiple npm workspaces for FOSSA scanning",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "echo \"no-op build\""
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "eslint": "^8.57.0",
    "prettier": "^3.2.5",
    "jest": "^29.7.0"
  }
}
